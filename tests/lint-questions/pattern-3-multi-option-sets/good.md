# Good fixture — pattern 3, single option set near a question

One option table feeding one atomic customer question:

| Storage | Notes |
| --- | --- |
| Redis | fast, ephemeral |
| Postgres | durable |

Which storage should we use?
