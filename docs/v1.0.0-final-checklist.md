# v1.0.0 Final Readiness Checklist

Objective gate set for promoting a tagged `v1.0.0-rc7` candidate to
`v1.0.0` final.
Owned by `project-manager`; release execution is owned by
`release-engineer`. This file is the source of truth for the
`final-ready` state used in `ROADMAP.md` and
`docs/v1.0-rc4-stabilization.md`.

Status: **not final-ready**.

## Gate Status

| Gate | Required evidence | Owner | Status | Evidence link / notes |
|---|---|---|---|---|
| G-1 Downstream clean window / sample | At least one downstream project upgrades to `v1.0.0-rc7` and completes the agreed clean window with no new contract, safety, privacy, or release-governance blocker. | `project-manager` + `qa-engineer` | Pending | Record downstream sample, window dates, and result here. |
| G-2 Zero final-blocking issues | Issues #71-#105 plus issue #116 are closed or explicitly downgraded by customer ruling; no new P0 / final-blocking issue remains open. | `project-manager` | Pending | rc6 closed the known #84, #104, and #105 blocker queue; rc7 candidate evidence remains required before final. |
| G-3 Smoke evidence | `scripts/smoke-test.sh` passes from the final rc7 candidate commit. | `qa-engineer` | Pending | rc6 smoke evidence is not sufficient for final after rc7 entered tag-prep. |
| G-4 Upgrade evidence | Fresh scaffold and supported upgrade paths pass from the rc7 candidate, including issue-feedback warning paths, rc6 issue #105 manifest verification, downstream-clean sample upgrade, and cross-harness validation where capabilities overlap. | `release-engineer` + `qa-engineer` | Pending | Must also record that issue #84 leaves `v1.0.0-rc3` immutable and relies on current/future bootstrap behavior plus the documented workaround for already-affected rc3-era trees. |
| G-5 Code-review sign-off | `code-reviewer` approves the final rc7 candidate commit set with no release-blocking findings. | `code-reviewer` | Pending | rc6 review evidence is not sufficient for the final rc7 candidate. |
| G-6 Release sign-off | `release-engineer` confirms clean worktree / reviewed rc7 commit set, changelog, release notes, and artifact state. | `release-engineer` | Pending | Candidate files are in tag-prep; no `v1.0.0-rc7` tag exists yet. |
| G-7 Customer ratification | Customer explicitly ratifies `v1.0.0` final from the review summary. | `tech-lead` | Pending | Customer answer must be recorded through the normal customer-record path. |
| G-8 GitHub Release object | After `v1.0.0` tag, create the GitHub Release object with release notes, upgrade notes, rollback notes, and issue closure links. | `release-engineer` | Pending | The rc cycle is tag-only; GitHub Release object starts at final. |
| G-9 Rollback / upgrade notes | Release notes include how downstream projects should upgrade, what to do on conflict, and how to roll back to `v1.0.0-rc7`, `v1.0.0-rc6`, or the prior known-good tag. | `release-engineer` + `tech-writer` | Pending | Existing release notes must be refreshed for the rc7 tag-prep boundary. |
| G-10 PM record closure | `docs/pm/CHANGES.md`, `docs/pm/LESSONS.md`, and `docs/pm/TOKEN_LEDGER.md` contain rc7 / final-relevant records or explicit no-data state. | `project-manager` | Pending | rc7 candidate-prep change record exists; final PM closure still requires lessons and token-ledger review. |
| G-11 Claude Code / Codex parity evidence | Final evidence includes both Claude Code and Codex validation for every release-relevant harness capability that overlaps, including issue #116 concise specialist briefs and the no-full-context-fork rule. | `project-manager` + `qa-engineer` + `release-engineer` | Pending | Claude Code validation evidence is not recorded yet. Validation from only one AI is insufficient for final while the template claims Claude Code / Codex parity. Exception requires unavailable harness capability, customer approval, date, and residual risk recorded here and in `docs/pm/CHANGES.md`. |

## Final Decision Rule

`v1.0.0` is `final-ready` only when every gate above is green or has an
explicit customer-approved exception. Final evidence must include both
Claude Code and Codex validation wherever their release-relevant harness
capabilities overlap; validation from only one AI cannot satisfy final.
Exceptions must name the waived gate, unavailable harness capability,
rationale, approver, date, and residual risk in this file and in
`docs/pm/CHANGES.md`.

## GitHub Release Object Steps

1. Confirm the final candidate commit is the reviewed rc7 commit set.
2. Create the annotated `v1.0.0` tag from that commit.
3. Push the tag after release-engineer approval.
4. Create the GitHub Release object for `v1.0.0`.
5. Include release notes with issue closure links for #71-#105 plus
   issue #116, upgrade notes, rollback notes, and
   downstream compatibility guidance.
6. Verify the Release object points to the intended tag and that issue
   references / closure comments are complete.

## Rollback / Upgrade Note Requirements

- Identify the previous known-good tag plus the `v1.0.0-rc7` and
  `v1.0.0-rc6` candidates as rollback targets until G-1 downstream
  clean-window / sample evidence is recorded as green.
- Tell downstream projects to run `scripts/upgrade.sh --dry-run`
  before applying the upgrade.
- Call out volatile-file and `.claude/agents/*-local.md` supplement
  behavior so project-specific routing survives upgrades.
- Note that issue #84 does not rewrite `v1.0.0-rc3`; if a `--dry-run`
  unexpectedly performed the upgrade in an rc3-era downstream tree,
  inspect the worktree diff, keep and commit only after review or
  restore from VCS, then use the final rc7 candidate
  `scripts/upgrade.sh --dry-run`
  from a clean branch/worktree for later previews.
- Require rerunning smoke tests and the issue-feedback onboarding check
  after upgrade.
