# Prior-art scan — T046

Owned by `researcher`. Produced at workflow-pipeline stage 1 for a triggered task.

---

## 1. Task reference

- **Task ID:** T046
- **One-line task statement:** Add agent contract check workflow in `.github/workflows/agent-contract-check.yml`.
- **Trigger clauses that fire:** 3 — cross-module boundary.

## 2. Search scope

- **Tier-1 (standards / official vendor docs):** GitHub Docs, "Workflow syntax for GitHub Actions," retrieved 2026-05-12; GitHub Docs, "Events that trigger workflows," retrieved 2026-05-12; GitHub Docs, "Secure use reference," retrieved 2026-05-12; `docs/workflow-pipeline.md`; `schemas/agent-contract.schema.json` task reference in `tasks.md`.
- **Tier-2:** none.
- **Tier-3:** none.

No local `.github/workflows/` files were present. `claude-mem` was not available in this harness.

## 3. Canonical solution

- **Canonical pattern found:** yes.

GitHub Actions workflows are YAML files stored in `.github/workflows`, with events, jobs, permissions, and steps declared in the workflow file. GitHub recommends least-privilege `GITHUB_TOKEN` permissions, careful handling of untrusted input, and pinning third-party actions where used. The task should wrap the existing contract lint/schema checks in a PR and manual CI workflow.

## 4. Candidate libraries / frameworks / APIs

| Candidate | Version | Licence | Maintenance signal | Fit / Rejection reason |
|---|---|---|---|---|
| GitHub Actions | SaaS feature | GitHub Terms | Official GitHub Docs current as of 2026-05-12 | Fits required workflow file. |
| Local shell lint scripts | Project scripts | Project licence | Existing task plan references contract linting | Fit as invoked commands inside the workflow. |
| Third-party validation action | unspecified | varies | unknown | Reject unless pinned and justified; built-in shell invocation is sufficient. |

## 5. Known pitfalls

- Workflow path filters can leave required checks pending if skipped unexpectedly.
- `pull_request_target` grants broader token access and should be avoided unless specifically needed and security-reviewed.
- Third-party actions should be pinned to full-length SHAs or avoided.

## 6. Recommendation to `architect` / `software-engineer`

Implement a minimal `pull_request` plus `workflow_dispatch` workflow with `permissions: contents: read`, checkout, and the local contract lint command; avoid secrets and third-party actions beyond pinned checkout.

## 7. Metadata

- **Last verified:** 2026-05-12.
- **Library versions cited:** GitHub Docs pages retrieved 2026-05-12; no package versions.
- **Retention:** durable. Archived only when the covered feature is removed from the project.
