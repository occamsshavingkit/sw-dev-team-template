# Prior-art scan - T039

Owned by `researcher`. Produced at workflow-pipeline stage 1 for a
triggered M6 task.

---

## 1. Task reference

- **Task ID:** T039
- **One-line task statement:** Implement contract linting in `scripts/lint-agent-contracts.sh`.
- **Trigger clauses that fire:** 3 cross-module boundary; 6 data-model change.

## 2. Search scope

- **Tier-1 (standards / official vendor docs):** JSON Schema Specification 2020-12, <https://json-schema.org/specification>, retrieved 2026-05-12; GitHub Actions workflow syntax, <https://docs.github.com/en/actions/using-workflows/workflow-syntax-for-github-actions>, retrieved 2026-05-12; local scripts under `scripts/`, local role files `.claude/agents/*.md`, `docs/workflow-pipeline.md`, retrieved 2026-05-12.
- **Tier-2:** Ajv CLI documentation, <https://ajv.js.org/packages/ajv-cli.html>, retrieved 2026-05-12.
- **Tier-3:** none.

`claude-mem` is not exposed in this harness; in-project prior-art was checked through current repository artifacts instead.

## 3. Canonical solution

- **Canonical pattern found:** yes.

Use a repository-local shell lint script that extracts structured contract data from canonical Markdown/frontmatter, validates it against JSON Schema, and reports actionable path/line failures while preserving current script conventions. Existing local scripts use Bash with explicit root checks and deterministic output; `scripts/lint-questions.sh` is warning-only prior art, while this M6 task likely needs fail-fast behavior for G6 evidence.

## 4. Candidate libraries / frameworks / APIs

| Candidate | Version | Licence | Maintenance signal | Fit / Rejection reason |
|---|---|---|---|---|
| ajv-cli | 5.0.0 | MIT | npm metadata modified 2023-04-28 | Fit: command-line validation with `validate`, `compile`, `test`; must use `--spec=draft2020`. |
| Ajv | 8.20.0 | MIT | npm metadata modified 2026-04-24 | Fit if script delegates to a small Node helper instead of ajv-cli. |
| POSIX shell only | n/a | n/a | Existing `archive-registers.sh` uses POSIX | Reject for parsing/frontmatter complexity unless no JSON validator is available. |
| Existing `scripts/audit-agent-tools.sh` | internal | project-created | Current script scans agent contracts heuristically | Fit as local style prior art, but should not remain heuristic-only for schema validation. |

## 5. Known pitfalls

- Draft mismatch: `ajv-cli` defaults to draft-07; M6 schemas should pass `--spec=draft2020`.
- Heuristic-only linting: existing tool audits implied tools, but contract lint must validate schema-shaped data and return non-zero on failures when used as gate evidence.
- Cross-module drift: lint must read canonical role files and schemas without editing generated outputs or treating runtime candidates as authority.

## 6. Recommendation to `architect` / `software-engineer`

Implement `scripts/lint-agent-contracts.sh` as a deterministic Bash script from repo root that extracts agent contract records, validates them with Ajv/ajv-cli against M6 schemas, prints concise file-scoped failures, and exits non-zero for G6-blocking contract violations.

## 7. Metadata

- **Last verified:** 2026-05-12
- **Library versions cited:** JSON Schema 2020-12; Ajv 8.20.0; ajv-cli 5.0.0.
- **Retention:** durable. Archived only when the covered feature is removed from the project.
