# Prior-art scan — T022

Owned by `researcher`. Produced at workflow-pipeline stage 1 for a
triggered M3 task.

---

## 1. Task reference

- **Task ID:** T022
- **One-line task statement:** Ensure repair or upgrade guidance creates missing `docs/intake-log.md` in `docs/TEMPLATE_UPGRADE.md`.
- **Trigger clauses that fire:** 3 — cross-module boundary.

## 2. Search scope

- **Tier-1 (standards / official vendor docs):** local canonical sources only: `specs/003-remaining-milestones/tasks.md:82`, `specs/003-remaining-milestones/spec.md:90-91,106`, `docs/TEMPLATE_UPGRADE.md:5-28,36-124`, `scripts/repair-in-place.sh:75-129,153-211,231-235`, `scripts/upgrade.sh:626-650,761-821,832-940`, `docs/templates/intake-log-template.md:12-22,53-80`, `docs/templates/qa/intake-conformance-template.md:42-44`, `sw_dev_template_implementation_plan-1.md:409-430`; read 2026-05-12.
- **Tier-2:** none consulted.
- **Tier-3:** none consulted.

## 3. Canonical solution

- **Canonical pattern found:** yes.
- The local upgrade model already adds upstream-shipped files that are missing from a scaffolded project (`scripts/upgrade.sh:644-650`) and documents per-version migrations for shape changes (`docs/TEMPLATE_UPGRADE.md:108-124`). The repair path also has a register-reset section but currently lists only `docs/OPEN_QUESTIONS.md`, `CUSTOMER_NOTES.md`, and `docs/AGENT_NAMES.md` (`scripts/repair-in-place.sh:90-95`). The canonical repair/upgrade guidance should therefore name `docs/intake-log.md` as a required project register and document that missing live intake logs are created from `docs/templates/intake-log-template.md` during repair or upgrade.

## 4. Candidate libraries / frameworks / APIs

| Candidate | Version | Licence | Maintenance signal | Fit / Rejection reason |
|---|---|---|---|---|
| `docs/TEMPLATE_UPGRADE.md` guidance update | n/a | project documentation | Existing canonical scaffold/upgrade doc | Required by task; documents expected behavior for maintainers and downstream projects. |
| `scripts/repair-in-place.sh` register-reset path | n/a | project script | Existing destructive repair tool | Strong fit for unzipped-in-place repairs; should create/reset `docs/intake-log.md` consistently with other registers. |
| `scripts/upgrade.sh` normal add-missing-file path | n/a | project script | Existing upgrade sync mechanism | Fit if `docs/intake-log.md` becomes upstream-shipped; may need migration or customization guidance for old projects with missing live logs. |

## 5. Known pitfalls

- Documentation-only repair is insufficient if downstream repositories rely on scripts; guidance should identify the script path or migration that actually creates the file.
- Destructive repair risk: `repair-in-place.sh` resets registers, so guidance must tell users to dry-run and preserve existing intake content if present.
- Upgrade preservation: an existing project-owned `docs/intake-log.md` should not be overwritten; missing files can be added, while existing logs should be preserved or added to `.template-customizations`.

## 6. Recommendation to `architect` / `software-engineer`

Document `docs/intake-log.md` as a required scaffolded register in `docs/TEMPLATE_UPGRADE.md`, with repair/upgrade guidance that creates it from the intake-log template when missing and preserves existing project logs; implementation should use the existing repair and upgrade/migration paths rather than a separate ad hoc procedure.

## 7. Metadata

- **Last verified:** 2026-05-12.
- **Library versions cited:** none.
- **Retention:** durable. Archived only when the covered feature is removed from the project.
