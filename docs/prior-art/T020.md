# Prior-art scan — T020

Owned by `researcher`. Produced at workflow-pipeline stage 1 for a
triggered M3 task.

---

## 1. Task reference

- **Task ID:** T020
- **One-line task statement:** Implement warning-only atomic-question lint checks in `scripts/lint-questions.sh`.
- **Trigger clauses that fire:** 3 — cross-module boundary.

## 2. Search scope

- **Tier-1 (standards / official vendor docs):** local canonical sources only: `specs/003-remaining-milestones/tasks.md:80`, `specs/003-remaining-milestones/spec.md:90-91,106`, `.claude/agents/tech-lead.md:26-56,61-77`, `docs/FIRST_ACTIONS.md:7-14,153-168`, `docs/templates/intake-log-template.md:53-75`, `docs/templates/qa/intake-conformance-template.md:21-45`, `sw_dev_template_implementation_plan-1.md:391-430`; read 2026-05-12.
- **Tier-2:** none consulted.
- **Tier-3:** none consulted.

## 3. Canonical solution

- **Canonical pattern found:** yes.
- The canonical local pattern is warning-only lint before hard gating. The source plan requires `scripts/lint-questions.sh` to flag compound seed questions, multiple numbered customer-question items, multiple independent option sets, `agents-running-at-ask` values other than `[]`, and compound-looking `OPEN_QUESTIONS.md` rows, with warning-only behavior for the first release cycle (`sw_dev_template_implementation_plan-1.md:391-407`). Current binding docs define the target rule: internal queues may batch rows, but customer-facing prompts must be one decision axis, one question, all agents/tools idle, and final on screen (`docs/FIRST_ACTIONS.md:7-14`; `.claude/agents/tech-lead.md:26-40`).

## 4. Candidate libraries / frameworks / APIs

| Candidate | Version | Licence | Maintenance signal | Fit / Rejection reason |
|---|---|---|---|---|
| Bash script using existing repo shell conventions | n/a | project MIT-compatible script style | Existing framework scripts are shell scripts under `scripts/` | Best fit: no new dependency; can scan canonical markdown and intake logs with standard shell tools. |
| External markdown linter | not selected | varies | not evaluated | Reject for this task: no external dependency is requested, and the checks are project-specific semantic rules, not generic Markdown style. |

## 5. Known pitfalls

- Over-enforcement too early: source plan says initial behavior is warning-only, then hard-gated after one release cycle; implementation should not fail CI for warnings yet.
- False positives from options: `docs/templates/intake-log-template.md:62-64` allows options for one decision axis; lint should warn on independent option sets, not every option list.
- Incomplete surface coverage: G3 acceptance requires historical bad patterns to be flagged; at minimum scan `docs/FIRST_ACTIONS.md`, `.claude/agents/tech-lead.md`, `docs/OPEN_QUESTIONS.md`, and `docs/intake-log.md` when present.

## 6. Recommendation to `architect` / `software-engineer`

Implement `scripts/lint-questions.sh` as a dependency-free warning-only shell check over the canonical question surfaces, emitting clear `WARN:` lines and exit 0 unless usage errors occur; defer hard-fail behavior to a later release-cycle task.

## 7. Metadata

- **Last verified:** 2026-05-12.
- **Library versions cited:** none.
- **Retention:** durable. Archived only when the covered feature is removed from the project.
