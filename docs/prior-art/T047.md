# Prior-art scan — T047

Owned by `researcher`. Produced at workflow-pipeline stage 1 for a triggered task.

---

## 1. Task reference

- **Task ID:** T047
- **One-line task statement:** Add question lint workflow in `.github/workflows/question-lint.yml`.
- **Trigger clauses that fire:** 3 — cross-module boundary.

## 2. Search scope

- **Tier-1 (standards / official vendor docs):** GitHub Docs, "Workflow syntax for GitHub Actions," retrieved 2026-05-12; GitHub Docs, "Events that trigger workflows," retrieved 2026-05-12; GitHub Docs, "Secure use reference," retrieved 2026-05-12; `docs/workflow-pipeline.md`; `scripts/lint-questions.sh` task reference in `tasks.md`.
- **Tier-2:** none.
- **Tier-3:** none.

No local `.github/workflows/` files were present. `claude-mem` was not available in this harness.

## 3. Canonical solution

- **Canonical pattern found:** yes.

GitHub Actions supports PR-triggered checks and path filtering, but GitHub warns skipped checks can remain pending when used as required checks. The project already has a warning-only atomic-question lint task, so the workflow should execute that script as visibility evidence without turning warnings into unintended merge blockers unless the script contract changes.

## 4. Candidate libraries / frameworks / APIs

| Candidate | Version | Licence | Maintenance signal | Fit / Rejection reason |
|---|---|---|---|---|
| GitHub Actions | SaaS feature | GitHub Terms | Official GitHub Docs current as of 2026-05-12 | Fits CI workflow file. |
| `scripts/lint-questions.sh` | Project script | Project licence | Existing completed T020 evidence | Fit as local lint implementation. |
| Third-party markdown/question linter | unspecified | varies | unknown | Reject: local lint behavior is already defined and avoids dependency risk. |

## 5. Known pitfalls

- If path filters omit a changed question surface, lint evidence can be missing.
- If warning-only output exits nonzero accidentally, the workflow can block PRs contrary to the task's earlier warning-only design.
- Inline shell should quote variables and avoid evaluating untrusted PR text directly.

## 6. Recommendation to `architect` / `software-engineer`

Add a minimal PR/manual workflow that runs `scripts/lint-questions.sh` with read-only permissions; keep it broad enough to cover docs and agent files where customer-facing questions live, and preserve warning-only semantics unless a later task changes the policy.

## 7. Metadata

- **Last verified:** 2026-05-12.
- **Library versions cited:** GitHub Docs pages retrieved 2026-05-12; no package versions.
- **Retention:** durable. Archived only when the covered feature is removed from the project.
