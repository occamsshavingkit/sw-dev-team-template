# Prior-art scan — T021

Owned by `researcher`. Produced at workflow-pipeline stage 1 for a
triggered M3 task.

---

## 1. Task reference

- **Task ID:** T021
- **One-line task statement:** Ensure fresh scaffold includes `docs/intake-log.md` via the existing scaffold template path documented in `docs/FIRST_ACTIONS.md`.
- **Trigger clauses that fire:** 3 — cross-module boundary.

## 2. Search scope

- **Tier-1 (standards / official vendor docs):** local canonical sources only: `specs/003-remaining-milestones/tasks.md:81`, `specs/003-remaining-milestones/spec.md:90-91,106`, `docs/FIRST_ACTIONS.md:7-14,153-168`, `docs/templates/intake-log-template.md:12-22,53-80`, `scripts/scaffold.sh:63-148,190-239,256-276`, `.claude/agents/tech-lead.md:46-56`, `docs/templates/qa/intake-conformance-template.md:21-45`, `sw_dev_template_implementation_plan-1.md:409-430`; read 2026-05-12.
- **Tier-2:** none consulted.
- **Tier-3:** none consulted.

## 3. Canonical solution

- **Canonical pattern found:** yes.
- The scaffold path already resets project-specific register stubs during `scripts/scaffold.sh` and seeds permanent customizations for those stubs. The missing canonical extension is to create `docs/intake-log.md` in the same scaffold-time register/stub path, using `docs/templates/intake-log-template.md` as the shape source. This aligns with `tech-lead` being required to append one intake-log entry for every customer question and QA expecting the file to exist and start at `turn: 1` (`.claude/agents/tech-lead.md:53-56`; `docs/templates/qa/intake-conformance-template.md:42-44`).

## 4. Candidate libraries / frameworks / APIs

| Candidate | Version | Licence | Maintenance signal | Fit / Rejection reason |
|---|---|---|---|---|
| Existing `scripts/scaffold.sh` register-stub path | n/a | project script | Current scaffold owns `docs/OPEN_QUESTIONS.md`, `CUSTOMER_NOTES.md`, and `docs/AGENT_NAMES.md` creation | Best fit: preserves existing scaffold mechanics and keeps intake log with project-specific registers. |
| Copy `docs/templates/intake-log-template.md` verbatim | n/a | project template | Template is current canonical shape | Accept with project-name substitution only if needed; avoid divergent hand-written shape. |
| Separate post-scaffold script | n/a | project script | not existing for this purpose | Reject: would split a required fresh-scaffold invariant away from the existing scaffold path. |

## 5. Known pitfalls

- Creating the archive but not the live file: `docs/intake-log-ARCHIVE.md` exists, but QA conformance requires live `docs/intake-log.md`.
- Forgetting `.template-customizations`: once populated, intake logs are project-specific append-only records and should be preserved like other scaffold stubs.
- Manifest drift: scaffold writes `TEMPLATE_MANIFEST.lock` after stubs are created, so the new live file must exist before `manifest_write` runs.

## 6. Recommendation to `architect` / `software-engineer`

Add `docs/intake-log.md` creation to `scripts/scaffold.sh` beside the existing register resets, preferably deriving from `docs/templates/intake-log-template.md`, and add it to `.template-customizations` before manifest generation so fresh scaffolds preserve project intake history.

## 7. Metadata

- **Last verified:** 2026-05-12.
- **Library versions cited:** none.
- **Retention:** durable. Archived only when the covered feature is removed from the project.
