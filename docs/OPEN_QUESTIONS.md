# Open Questions register

Tracks every open question on the project. Steward: `researcher`.
`tech-lead` opens items; the named answerer closes them.

Columns:

- **ID** — `Q-NNNN`, monotonic.
- **Opened** — ISO date.
- **Question** — single sentence, sharp enough to answer.
- **Blocked on** — what cannot proceed until this is answered.
- **Answerer** — `customer` / `tech-lead` / `architect` / `researcher` / `sme-<domain>` / agent name.
- **Status** — `open` / `answered` / `deferred` / `withdrawn`.
- **Resolution** — verbatim answer (if from customer, mirror into `CUSTOMER_NOTES.md`) + date.

Each row records exactly one decision axis. Internal planning queues may
hold multiple related rows, but do not merge independent decisions into a
single `Question` cell. Ask the customer **one question per idle turn**,
only when all agents are idle (so the question does not scroll off
screen). See `.claude/agents/tech-lead.md` Job #1 and `CLAUDE.md` Step 2.

Archive pointer: historical snapshots live in `docs/OPEN_QUESTIONS-ARCHIVE.md`.
Keep open and recently answered rows here; use `scripts/archive-registers.sh`
for append-only archival before replacing historical live content with a compact
tombstone.

| ID | Opened | Question | Blocked on | Answerer | Status | Resolution |
|---|---|---|---|---|---|---|
| Q-0001 | 2026-04-19 | For PMBOK role placement, do we add a dedicated `project-manager.md` agent, harden `tech-lead.md` to own PMBOK, or both? | Template roster shape | customer | answered | **(a)** New dedicated `project-manager.md`; `tech-lead` stays technical. Customer, 2026-04-19. |
| Q-0002 | 2026-04-19 | Glossary split — target layout: `docs/glossary/ENGINEERING.md` + `docs/glossary/PROJECT.md`, with existing `docs/GLOSSARY.md` deleted and all references updated. Confirm? | Glossary directory work | customer | answered | Confirmed by customer directive 2026-04-19 (*"we need a glossary directory … with a name like ENGINEERING and a project glossary for project jargon"*). Executed 2026-04-19: `docs/GLOSSARY.md` → `docs/glossary/ENGINEERING.md`, new `docs/glossary/PROJECT.md` stub, all 11 references updated. |
| Q-0003 | 2026-04-19 | Which PMBOK artifacts should the new `project-manager.md` agent own as first-class deliverables? | Scope of `project-manager.md` | customer | answered | Superseded by Q-0010 and resolved by milestone scope (c) — full PMBOK seven adopted as template default. |
| Q-0004 | 2026-04-19 | `sme-template.md` exists but `CLAUDE.md` never mentions it. Add a §SME creation section to `CLAUDE.md` that points at the template? | CLAUDE.md completeness | tech-lead | answered | Executed 2026-04-19: `sme-template.md` added to roster table; new "Creating an SME agent" section added to `CLAUDE.md`. |
| Q-0005 | 2026-04-19 | Move CLAUDE.md Step-2 scoping-question text into `docs/templates/scoping-questions-template.md` so it is maintainable separately? | Template structure | tech-lead | answered | Executed 2026-04-19: created `docs/templates/scoping-questions-template.md`; `CLAUDE.md` Step 2 now references it. |
| Q-0006 | 2026-04-19 | Add a `docs/INDEX.md` table-of-contents for all docs? | Doc discoverability | tech-lead | answered | Executed 2026-04-19: `docs/INDEX.md` created covering repo root, `docs/`, `docs/glossary/`, `docs/pm/`, `docs/sme/`, `docs/templates/`, and `.claude/agents/`. |
| Q-0007 | 2026-04-19 | Define explicit "definition of done" for the scoping conversation itself. | Scoping gate | customer | answered | Option (iii) — checklist — customer, 2026-04-19. Codified in `CLAUDE.md` Step 2 "Definition of Done" checklist (project summary, SME classification, first milestone, escalation paths, Step-3 naming, project charter, open-questions register). |
| Q-0008 | 2026-04-19 | Install Trail of Bits skills into this session via `/plugin marketplace add ~/.claude/trailofbits-skills` + `/plugin install trailofbits-skills@trailofbits-skills`? Customer must type these. | ToB availability this session | customer | answered | Yes — customer, 2026-04-19 (*"install trail of bits into this session too"*). Repo cloned to `~/.claude/trailofbits-skills`; customer types the two `/plugin` slash commands themselves. |
| Q-0009 | 2026-04-19 | Pick a naming category (or custom list) for the team. | Agent-team panel readability | customer | answered | **Muppets** — customer, 2026-04-19 (*"muppets has been fun"*). Natural-balance rule applied (no artificial over-representation of minority gender). Mapping filed in `/home/quackdcs/SWEProj/docs/AGENT_NAMES.md` (this project) and exemplified in the template's `docs/AGENT_NAMES.md`. |
| Q-0010 | 2026-04-19 | Which PMBOK artifacts should `project-manager` own immediately vs. defer? | PM agent scope | customer | answered | Milestone scope (c), 2026-04-19: seed all seven PMBOK artifact templates in `docs/templates/pm/`. Done 2026-04-19: CHARTER / STAKEHOLDERS / SCHEDULE / COST / RISKS / CHANGES / LESSONS templates in place; `project-manager.md` now points at them. |
| Q-0011 | 2026-04-19 | What is the v1 milestone for this template-improvement project? | Milestone definition | customer | answered | Option (c), 2026-04-19: (a) all OPEN_QUESTIONS closed + (b) `docs/templates/pm/` artifact templates seeded + (c) dry-run on a throwaway new project to prove scoping flow end-to-end. |
| Q-0012 | 2026-04-19 | Does this project participate in upstream issue feedback (Step 4)? | Gap-reporting convention | customer | answered | **Yes** — customer, 2026-04-19. Stamped `TEMPLATE_VERSION v0.1.0 / 0700927` at project root. Initial gaps filed as issues #1 (scaffold script) and #2 (pronoun verification) against `occamsshavingkit/sw-dev-team-template`. |
| Q-0013 | 2026-04-20 | Issue #19 — PM no-customer-contact rule vs. respawn-notification carve-out. Pick: (A) narrow explicit exception for PM during tech-lead respawn; (B) respawned tech-lead announces itself (cleanest re "sole interface" invariant); (C) customer notices via tech-lead's "what I believe is true" summary, no announcement. | `v0.10.1` PATCH for PM respawn flow | customer | answered | **Fix B** — customer, 2026-04-20. Implemented: `project-manager.md` no longer contacts customer at respawn; `agent-health-contract.md` § 5.4 rewritten with "First-turn customer message" discipline; `tech-lead.md` § "Agent health + respawn" updated. The handover-brief template should grow a "First-turn customer message" section (next-session TODO). |
| Q-0014 | 2026-04-20 | Issue #15 — rename `customer` → `product owner` across template. Breaking; scheduled as `v0.11.0` MINOR with migration script. Confirm rename and confirm whether `CUSTOMER_NOTES.md` becomes `PRODUCT_OWNER_NOTES.md` or the more neutral `DECISIONS.md` / `NOTES.md`. | `v0.11.0` rename | customer | answered | **No rename.** Keep `customer` as the umbrella term; instead, make the glossary explicitly define **product owner**, **end customer / end user**, **sponsor**, and **SME** (four roles, one person can wear several). Customer ruling 2026-04-20. Executed: `sw-dev-team-template/docs/glossary/ENGINEERING.md` § "Roles and parties" rewritten with all four definitions + explicit "role-stacking" paragraph explaining how a single human can simultaneously occupy multiple roles. Upstream issue #15 gets closed with this as the resolution (non-rename). |
| Q-0015 | 2026-04-20 | v0.10.0 pre-commit smoke test (B-2): run `scripts/scaffold.sh /tmp/smoke-test-v010 "Smoke Test"` and confirm it completes cleanly against the new VERSION. Bash permission denied in this session — customer must run. | v0.10.0 tag | customer | answered | **Clean ✓** — customer ran 2026-04-20. Scaffold emitted "Scaffolded 'Smoke Test' at /tmp/smoke-test-v010; Template v0.10.0 (f78ab30); Stamped 2026-04-20" and the FIRST ACTIONS next-steps banner. No errors. v0.10.0 is clear to commit + tag. |
| Q-0016 | 2026-05-14 | Customer flagged that the sub-repo's recording of the one-question-per-turn rule "isn't strong enough." Rule is currently in CLAUDE.md, AGENTS.md, .claude/agents/tech-lead.md, docs/FIRST_ACTIONS.md, docs/OPEN_QUESTIONS.md, docs/templates/intake-log-template.md, scripts/lint-questions.sh (warns-only — HARDGATE_AFTER_SHA=DEFERRED_SET_AT_HARDGATE_PR). Which dimension to strengthen: (A) flip lint-questions to hard-fail (set HARDGATE_AFTER_SHA to a real SHA); (B) add a session-start hook that echoes the rule back so the assistant must acknowledge it before tool use; (C) promote the rule to a numbered Hard Rule in CLAUDE.md / AGENTS.md and reference it from every agent contract; (D) all of the above? | rule enforcement | customer | answered | **D — all three** — customer, 2026-05-14. Shipped on `feat/pre-release-gate` branch in `sw-dev-team-template/` at commit `c44c104`: (A) `scripts/lint-questions.sh` `HARDGATE_AFTER_SHA` flipped from `DEFERRED_SET_AT_HARDGATE_PR` to `fcd3e2e71c415755b5a79a7b73c21eea318f67de` (rc10 commit); `.github/workflows/question-lint.yml` updated to invoke with `--since HARDGATE_AFTER_SHA` so new lines added after the cutoff hard-fail CI. (B) `scripts/hooks/atomic-question-reminder.sh` (new) prints Hard Rule #11 at session start; wired into `.claude/settings.json` SessionStart hook alongside version-check. (C) Hard Rule #11 added to `CLAUDE.md` with explicit strict-reading clarification ("multi-select" framing IS the violation regardless of customer answering "all of the above"); AGENTS.md inherits via its existing "read CLAUDE.md" routing contract. Verified: `lint-questions.sh --since HARDGATE_AFTER_SHA --summary` reports 0 errors at HEAD; SessionStart hook prints the 4-line rule text. Plus auto-memory `feedback_atomic_questions_strict.md` records the strict-reading lesson for future-self. |
| Q-0018 | 2026-05-14 | Customer pushback during PR #162 work: *"hard rules seem already to get forgotten, and making more of them will make it worse."* Hard Rule #11 (atomic customer questions) shipped in commit `c44c104` per explicit customer choice D on Q-0016. The new pattern (no Hard Rule #12 for agent backgrounding — keep that one in `.claude/agents/tech-lead.md` only) is settled. Open question: should Hard Rule #11 be retroactively demoted to the tech-lead role contract too, given the broader proliferation concern? Demotion means moving the strict-atomic clarification + the binding language from CLAUDE.md hard-rules list into `.claude/agents/tech-lead.md`; the lint hard-gate (A) and SessionStart hook (B) reinforcement stay regardless. | rule-set hygiene | customer | open | — |
| Q-0019 | 2026-06-03 | fw-adr-0024 / #212: which parallel-agent isolation strategy to adopt for readers vs writers sharing the canonical checkout? | parallel-agent safety | customer | answered | Hybrid adopted: readers (code-reviewer/researcher) run in throwaway git worktrees; writers serialized on the canonical checkout via a writer-lane token. Implement the 11 enumerated contract/helper changes. fw-adr-0024 → Accepted. Customer, 2026-06-03. Note: no matching intake-log entry exists; flagged to tech-lead. |
| Q-0020 | 2026-06-03 | fw-adr-0023 / #276: how to address unbounded growth of the handoff activity array? | handoff schema size | customer | answered | Sidecar adopted: move `activity` to a gitignored `docs/handoffs/<task>.activity.jsonl`; MINOR schema bump + one-time migration; removes the hash-exclusion normalizer. fw-adr-0023 → Accepted. Customer, 2026-06-03. Note: no matching intake-log entry exists; flagged to tech-lead. |
| Q-0021 | 2026-06-03 | #301 (P13): should a ui-ux-designer role be added as the 14th canonical role? | roster completeness | customer | answered | Add ui-ux-designer role (14th canonical role; owns UX design + WCAG/accessibility; likely wraps accesslint). Customer, 2026-06-03. Note: no matching intake-log entry exists; flagged to tech-lead. |
| Q-0022 | 2026-06-03 | #290: should an mcp-liaison role be added to own delegated external-model MCP sessions? | roster completeness | customer | answered | Add mcp-liaison role (owns delegated external-model MCP sessions; brief construction + divergence reconciliation). Customer, 2026-06-03. Note: no matching intake-log entry exists; flagged to tech-lead. |
| Q-0023 | 2026-06-03 | #291: should researcher be split into two roles — investigation vs record custodian? | researcher role scope | customer | answered | Split researcher into researcher (investigation) + librarian (record custodian: CUSTOMER_NOTES, OPEN_QUESTIONS, glossary, SME inventory, archival). Customer, 2026-06-03. Note: no matching intake-log entry exists; flagged to tech-lead. |
| Q-0024 | 2026-06-03 | #302 (P14): should a project-tailoring/integrity-level artifact be added to support non-uniform ceremony? | ceremony policy | customer | answered | Keep uniform ceremony — decline project-tailoring/integrity-level artifact; close #302 won't-fix. Customer, 2026-06-03. Note: no matching intake-log entry exists; flagged to tech-lead. |
| Q-0025 | 2026-06-03 | #303 (P15): should a clarification-session mode be documented that relaxes HR-11 cadence within an opted-in session? | HR-11 / question cadence | customer | answered | Document a clarification-session mode: inside an opted-in session, tech-lead asks sequential one-axis questions back-to-back (relaxing HR-11 cadence) while each stays atomic (one decision axis); shape rule stays binding. Customer, 2026-06-03. Note: no matching intake-log entry exists; flagged to tech-lead. |
| Q-0026 | 2026-06-03 | #287: how should specialists handle unbounded polling or long-running background tasks? | specialist loop safety | customer | answered | Bounded + harness mechanism: specialists must not poll-loop unbounded; cap and escalate to tech-lead, with a background-task auto-re-invoke pattern (ties to #265). Customer, 2026-06-03. Note: no matching intake-log entry exists; flagged to tech-lead. |
| Q-0027 | 2026-06-03 | #293: should delegated-specialist mode be added to all supported harness providers, or only AGENTS.md? | multi-harness parity | customer | answered | Add delegated-specialist mode across ALL supported providers (AGENTS.md/Codex, GEMINI.md/Gemini, opencode, etc.), not just AGENTS.md. Customer, 2026-06-03. Note: no matching intake-log entry exists; flagged to tech-lead. |
| Q-0028 | 2026-06-03 | #300 (P12): should the Gemini full-team harness be built now standalone, or deferred until delegated-specialist parity is resolved? | Gemini harness sequencing | customer | answered | Build the Gemini full-team harness now, standalone (GEMINI.md + .gemini/agents roster); retrofit delegated-specialist parity (#293) afterward. (fw-adr-0022 already accepted.) Customer, 2026-06-03. Note: no matching intake-log entry exists; flagged to tech-lead. |
| Q-0029 | 2026-06-03 | #294 (P1): for preventing duplicate ADR/spec/Q IDs, should the solution be a script mechanism, a CI gate, or both? | ID-collision prevention | customer | answered | Both: auto-allocate next-free IDs via reserve-number.sh (mechanism) AND a CI backstop that fails on duplicate ADR/spec/Q IDs. Customer, 2026-06-03. Note: no matching intake-log entry exists; flagged to tech-lead. Also note: META and SCAFFOLD OPEN_QUESTIONS files have diverged Q-numbering — the scaffold's #278 used Q-0019..Q-0022 for different questions; cleanup tied to #294. |
| Q-0030 | 2026-06-03 | #297 (P9): should one-task-per-dispatch be enforced via a dispatch-template mechanism, a CI gate, or both? | dispatch discipline | customer | answered | Dispatch-template mechanism only: structure dispatch so one-task-per-dispatch is the default shape (no CI gate — dispatch is ephemeral). Customer, 2026-06-03. Note: no matching intake-log entry exists; flagged to tech-lead. |
| Q-0031 | 2026-06-03 | #292: for improving CUSTOMER_NOTES integrity, should the solution be a structured entry template, a content-aware guard, or both? | CUSTOMER_NOTES integrity | customer | answered | Both: structured CUSTOMER_NOTES entry template (mechanism) AND make customer-notes-guard.py content-aware (flag oversized/off-scope/unstructured entries). Customer, 2026-06-03. Note: no matching intake-log entry exists; flagged to tech-lead. |
| Q-0032 | 2026-06-03 | #299 (P11): should a rule-authoring enforcement checklist be binding or non-binding guidance? | rule authoring hygiene | customer | answered | Authoring checklist (non-binding): a guidance checklist reminding rule-authors to consider enforcement — NOT a new binding rule (respects the Q-0018 anti-proliferation concern). Customer, 2026-06-03. Note: no matching intake-log entry exists; flagged to tech-lead. |
| Q-0017 | 2026-05-14 | Pre-release gate's `upgrade-paths` sub-gate flags 29/38 source tags as failing (mostly v0.x cross-MAJOR + v1.0.0-rc1/rc2). **Diagnosis (2026-05-14 update):** failures are REAL bugs, not gate setup. Two distinct failure modes confirmed: (1) **v0.x → modern self-overwrite crash** — v0.x upgrade.sh has no bootstrap, the sync loop overwrites scripts/upgrade.sh mid-run, bash continues reading from the corrupted byte offset (e.g., v0.10.0 fails with `line 205: y: command not found` — the "y" is the suffix of "Manifest integrity drift takes precedence" comment from modern upgrade.sh at the byte position bash was reading); the prior manual repro that appeared to succeed was masked by a `\| tail` wrapper. (2) **v0.17.x → rc10 tag-discovery failure** — bootstrapped clone reports `--target v1.0.0-rc10 is not a known tag` (different bug). Both are upgrade-path infrastructure rot that has never been exercised. Strategy: (A) fix the underlying upgrade-self-overwrite by making modern upgrade.sh defer scripts/upgrade.sh + scripts/lib/manifest.sh sync to the END of the sync loop, AND fix v0.17.x tag-discovery (big, multi-day); (B) maintain `tests/release-gate/upgrade-paths-allowlist.txt` for tags where cross-MAJOR upgrade is documented-broken — surfaces stay logged but don't fail the gate; (C) narrow FR-003 scope from "every published tag" back to "every rc tag on current MAJOR + latest stable below" (overrides Clarifications Session 2026-05-14 Q2=D); (D) combination — allowlist the v0.x-class breakage now (with rc-bump notes), file the underlying upgrade.sh fix as a separate upstream issue, keep rc tags strict. | rc11 cut | customer | answered | **A then B** — customer, 2026-05-14. (A) Fix the self-overwrite bug landed in commit `19e39bc` (`migrations/v0.14.0.sh` pre-bootstrap that atomic-renames the candidate's upgrade.sh + scripts/lib/* into the project before v0.x's sync loop runs; bash's open fd on the original inode survives, sync sees cmp-equal and skips cp). Then commit `ab7b49a` narrowed the gate's `upgrade-paths` scope to SWDT_UPSTREAM_URL-aware source tags (added in v0.16.0 / v1.0.0-rc3) — the 27 pre-SWDT_UPSTREAM_URL tags hardcode the GitHub URL and cannot be exercised against an unpublished local candidate, so they're out-of-scope by technical constraint. Gate now exercises 10 source tags (v0.16.0, v0.17.0, v1.0.0-rc3..rc10). (B) v0.16.0 still failed verify (1 unresolved conflict on scripts/upgrade.sh itself, three-way diff between baseline / candidate / pre-bootstrapped state); allowlisted via `tests/release-gate/upgrade-paths-allowlist.txt` in commit `f2be37e`. Follow-up: teach v0.x conflict-detection to recognise pre-bootstrapped helpers as accepted-local-merge so v0.16.0 can come off the allowlist. Final gate state: 9/10 round-trips pass, 1 (v0.16.0) allowlisted-logged, sub-gate exits 0. |
