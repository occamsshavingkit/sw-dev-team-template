# docs/pm/token-ledger/prompts/

Optional verbatim-prompt archive supporting token-ledger entries per FR-005. Classification: **ephemeral** — entries here are kept only when a ledger row needs literal prompt text for audit or reproduction, and are pruned alongside their parent ledger row. File naming: `<task-id>-<agent>.md` (e.g., `T012-software-engineer.md`). One prompt per file; do not concatenate or summarize.
