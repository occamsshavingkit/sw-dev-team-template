# docs/runtime/agents/

Holds compact runtime contracts emitted for each agent role at build time. Classification: **generated** — do not hand-edit; regenerate from source instead. Canonical input: `.claude/agents/<role>.md`. Generator: `scripts/compile-runtime-agents.sh` (arrives at T012). Files here are the trimmed contracts the running harness loads at dispatch time; the full human-readable manuals live under `docs/agents/manual/`.
